{
  "name": "blg-portal-site",
  "version": "1.0.0",
  "description": "BLG Portal Site - Static website for portal functionality",
  "main": "public/index.html",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npm run preview",
    "deploy": "npm run build && node scripts/deploy.js",
    "quick-deploy": "npm run build && git add . && git commit -m \"Quick update\" && git push origin main",
    "serve": "python3 -m http.server 8000 --directory public",
    "preview": "npm run serve"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hnarita-blg/blg-portal.git"
  },
  "keywords": [
    "portal",
    "static-site",
    "github-pages"
  ],
  "author": "BLG",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}